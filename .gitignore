# build staging — the published copy at the repo root IS committed
# (index.html, _astro/, favicon.svg, .nojekyll), since GitHub Pages serves
# this repo from "main / (root)".
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
# environment
.env
.env.production
# macOS
.DS_Store
